FROM openjdk:24-jdk-slim AS builder
WORKDIR /app
COPY . .
RUN chmod +x gradlew
RUN ./gradlew :reactor-app:bootJar --no-daemon

FROM openjdk:24-slim
WORKDIR /app
COPY --from=builder /app/reactor-app/build/libs/*.jar app.jar
ENTRYPOINT ["java", "-Xmx256m", "-Xms256m", "-jar", "app.jar"]
